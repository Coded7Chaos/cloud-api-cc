@extends('layouts.app')
@section('title','Seguimiento')
@section('content')
<header class="mb-5"><h1 class="text-2xl font-semibold text-[#004479]">Seguimiento de agentes</h1><p class="text-sm text-slate-500">Tareas realizadas y actividad de respuestas.</p></header>
<section class="mb-4 rounded-2xl bg-white overflow-hidden"><h2 class="border-b px-5 py-4 font-semibold text-[#004479]">Historial de tareas</h2><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Tarea</th><th class="p-4">Agente</th><th class="p-4">Asignada</th><th class="p-4">Estado</th><th class="p-4">Realizada</th></tr></thead><tbody class="divide-y">@forelse($assignments as $row)<tr><td class="p-4 font-medium">{{ $row['task']->titulo }}</td><td class="p-4">{{ $row['user']->name }} {{ $row['user']->last_name }}</td><td class="p-4">{{ $row['user']->pivot->created_at?->format('d/m/Y H:i') }}</td><td class="p-4">{{ $row['user']->pivot->status==='completed'?'Realizada':'Pendiente' }}</td><td class="p-4">{{ $row['user']->pivot->completed_at ? \Illuminate\Support\Carbon::parse($row['user']->pivot->completed_at)->format('d/m/Y H:i') : '—' }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">Sin asignaciones.</td></tr>@endforelse</tbody></table></div></section>
<section class="rounded-2xl bg-white overflow-hidden"><h2 class="border-b px-5 py-4 font-semibold text-[#004479]">Actividad de chats</h2><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-4">Agente</th><th class="p-4 text-center">Chats asignados</th><th class="p-4 text-center">Respuestas</th><th class="p-4">Última respuesta</th></tr></thead><tbody class="divide-y">@foreach($agents as $agent)<tr><td class="p-4"><strong>{{ $agent->name }} {{ $agent->last_name }}</strong><small class="block text-slate-500">{{ $agent->email }}</small></td><td class="p-4 text-center">{{ $agent->conversations_count }}</td><td class="p-4 text-center">{{ $agent->responses_count }}</td><td class="p-4">{{ $agent->last_response_at?\Illuminate\Support\Carbon::parse($agent->last_response_at)->format('d/m/Y H:i'):'Sin actividad' }}</td></tr>@endforeach</tbody></table></div></section>
@endsection
